# Ayoub Infrastructure (Cold Black)

A personalized, cold black themed infrastructure-style aviation view inspired by:
`https://nebel2613.github.io/infrastructure/?v=2`

## Run locally

Open `index.html` directly in your browser, or run:

```bash
python3 -m http.server 8000
```

Then visit `http://localhost:8000`.
